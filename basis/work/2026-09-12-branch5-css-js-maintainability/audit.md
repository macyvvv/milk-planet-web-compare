# Independent audit

This is a non-independent same-agent post-build review. It is recorded as a required visual-fidelity gate and is not presented as an independent human approval.

## Compared sources

- `AGENTS.md`, `basis/README.md`, `basis/WBS.md`, `basis/current_state.md`, `basis/system_spec.md`, and `DESIGN.md`
- Branch5 CSS/JS inventory and `consumer-matrix.md`
- Prior shared-base boundary and maintainability packets, including their known regression and timeout risks
- `proposal/branch5` HTML, CSS, JavaScript, vendor references, and current working-tree diff

## Viewports

- PASS (visual) — Shinjuku shop top was inspected at 390px, 768px, and 1440px. The hero, SHOP heading, access map, store information, and responsive composition remained coherent; no visible horizontal clipping was observed. At 390px, the document scrollWidth matched innerWidth (390).
- PASS (structure) — Shinjuku at 390px exposed one slider, one initialized Slick slider, 16 gallery links, two nav elements, one h1, and the analytics initialization guard.
- PASS (visual/structure) — the Shinjuku HTML menu was inspected at 390px, 768px, and 1440px. Its skip link, menu navigation, pricing system, and responsive two-column-to-single-column behavior were present.
- PASS (representative families) — root, shop list, cast, recruit, planet-group, and okyuji routes were opened at 390px. Each exposed the expected primary structure and no browser console errors were recorded.
- PASS (store variant) — Bloody shop top was opened at 390px. Store-specific imagery and SHOP structure remained present; no browser console errors were recorded.
- PARTIAL — automated width metrics at 768px and 1440px were not collected because the browser protocol intermittently timed out while evaluating page DOM. Screenshots were captured and visually inspected at those widths instead.

## Static checks

- PASS — `python3 tools/validate_repo_contract.py` after the branch5 scope synchronization.
- PASS — `python3 tools/validate_static_contract.py proposal/branch5` baseline.
- PASS — `python3 tools/validate_skill_packages.py` baseline.
- PASS — `python3 tools/validate_work_packets.py` after this packet and implementation changes.
- PASS — `node --check` for changed files and all executable Branch5 JS, excluding the nine known store-local `.js` paths containing captured 404 HTML. Those nine paths remain separately reported and were not silently treated as JavaScript.
- PASS — `git diff --check` after implementation.
- PASS — `python3 tools/validate_repo_contract.py` after implementation.
- PASS — `python3 tools/validate_static_contract.py proposal/branch5` after implementation.
- PASS — `python3 tools/validate_skill_packages.py` after implementation.

## Browser observations

- The repository-root local server resolved vendor-relative assets correctly for the inspected Shinjuku and Bloody pages.
- Browser console error logs were empty for the representative routes. This does not prove that every generated or externally fetched asset completed successfully.
- The server log exposed pre-existing 404s generated by proposal/branch5/planet-group/shop.js for 13 store image paths under proposal/branch5/planet-group/images/ (milk.jpg, cybar.jpg, chocolat.jpg, cphanare.jpg, shandy.jpg, bloody.jpg, melty.jpg, roysuga.jpg, tweeny.jpg, cybarb.jpg, cybarb2.jpg, cybarl.jpg, and planetplanet.jpg). The implementation batch did not alter planet-group or these assets. They are recorded as an existing content/asset defect, not as a refactor regression.
- External fonts, widgets, and remote resources were not used as completion evidence. Their timeout or availability remains a separate status.

## Accessibility and content checks

- PASS (regression scope) — no content, URL, title, alt text, menu variant, or store-specific expression changed in the implementation diff.
- PASS — the Shinjuku DOM snapshot retained the visible store title, access information, related links, event labels/URLs, and gallery alt text.
- PASS — the HTML menu DOM snapshot retained the skip link, in-page anchors, pricing content, and menu labels.
- PARTIAL — keyboard activation and formal W3C validation were not run; no markup or visual content change was included in this batch.

## Findings

- PASS — the changed shared JS preserves existing slider options, modal labels/options, generated event labels/URLs, analytics payload construction, gallery selector boundaries, and script load order.
- PASS — absent jQuery/Slick/Modaal dependencies now fail closed for the relevant initialization path instead of throwing; repeated Slick/Modaal/gallery/analytics setup is guarded.
- PASS — eventlist.js no longer writes j as an undeclared global and no longer carries dead showslide/document.write experiments.
- ADVISORY / Parking Lot — CSS/JS inventory and consumer matrix still show a high-risk shared cascade and legacy artifacts. proposal.css has 343 !important declarations and Branch5 totals 1,092; cascade reduction and CSS decomposition remain separate work.
- RESOLVED by `2026-09-12-branch5-planet-group-asset-recovery` — the 13 planet-group JPGs were absent from the local capture but remain available at the original legacy endpoints. Exact JPEGs were recovered into Branch5 without changing the renderer. The four PNG files remain a separate unresolved CSS/asset issue because their original endpoints still return 404.
- ADVISORY — nine store-local eventlist.js paths are captured 404 HTML snapshots, not executable scripts. Their references occur in HTML comments in legacy JPEG-menu pages, while current shop-top pages load the root eventlist.js. They are retained because deletion of comparison assets was not authorized.
- ADVISORY / Parking Lot — literal unreferenced candidates (common.js, legacy minified CSS, shop_menu_override.css, and similar assets) remain undeleted until direct URL, Git-history, and comparison-asset checks are completed.
- ADVISORY / Parking Lot — document.write remains in legacy cast/event/planet-group rendering. It is parser-order-sensitive and requires a separate route-by-route replacement test.

## Residual risks

- External fonts, widgets, and vendor scripts can prevent complete browser load states.
- Broad `!important` precedence and differing relative CSS load orders remain unchanged.
- `document.write`-based scripts and legacy menu snapshots remain high-risk behavior boundaries.
- Formal W3C, real-network performance, and business KPI validation remain outside this packet.
- Browser audit is non-independent and includes a protocol-timeout limitation for some automated width metrics.
- Release status is intentionally local-only; no publish action is missing from this request.

## Sequential perspective review

- Strategy: Proceed with the bounded low-risk batch; it reduces regression exposure without spending the current change on high-risk cascade cleanup.
- Business/process: Proceed; preserving page content and store identity protects conversion paths while reducing avoidable runtime failures. The 13 missing planet-group images require a separate owner/content decision.
- Architecture: Proceed; shared script responsibilities and existing load order remain intact; no new abstraction or dependency was introduced.
- Development: Proceed; changed JS passes syntax checks, removes the implicit global, and uses explicit initialization guards.
- Security: Proceed; no new external communication, dependency, permission, or data sink was introduced. Existing analytics and third-party resources are unchanged.
- Data/content: Proceed with risk noted; generated event and store data strings remain unchanged, while missing planet-group image assets are not guessed.
- DevOps/environment: Proceed; local checkpoints and rollback commits exist, validators are reproducible, and nothing was pushed or published. Browser testing used a repository-root server.
- UI/UX: Proceed for this batch; screenshots at 390/768/1440 preserved the existing Branch5 visual direction and no new generic styling was introduced.
- Knowledge: Proceed; the Branch5 scope and work packet are synchronized, and historical branch references remain historical rather than being rewritten.
- Quality: Improve and proceed; static checks pass, representative browser checks pass or are explicitly partial as recorded, while formal accessibility and complete width metrics remain unexecuted.
- Operations: Proceed locally; rollback is available, but ownership/freshness for legacy assets and external services remains unknown.
- Integrated decision: `IMPROVE_AND_PROCEED` for the current local implementation only. Do not promote to independent approval, merge, publish, or release without resolving or explicitly accepting the recorded residual risks.
