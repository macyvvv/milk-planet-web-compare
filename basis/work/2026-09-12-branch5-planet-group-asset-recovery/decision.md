# Decision

## Scope

Add the 13 exact JPEG files generated by `proposal/branch5/planet-group/shop.js` from the original site's legacy `planet-group` image endpoints. Do not modify the renderer or any shared CSS/JS.

## Target files

- `proposal/branch5/planet-group/images/{milk,cybar,chocolat,cphanare,shandy,melty,bloody,roysuga,tweeny,cybarb,cybarb2,cybarl,planetplanet}.jpg`
- `basis/work/2026-09-12-branch5-planet-group-asset-recovery/`
- `basis/decision_log.md`, `basis/WBS.md`, and `basis/current_state.md`

## State

State: IMPLEMENTED

## Gate status

- Source lock: PASS — all 13 original endpoints returned valid JPEG responses.
- Content and task: PASS — exact legacy filenames are restored; no guessed substitutions are used.
- Structure: PASS — the existing `shop.js` filename contract is unchanged.
- Intent: PASS — store order, identity, layout, and URLs are unchanged.
- Security: PASS — no dependency, script, or executable content was added.
- Independent audit: PASS (NON-INDEPENDENT SAME-AGENT REVIEW) — retrieval, signature, dimension, reference, and contract checks are recorded in `audit.md`.
- Release: NOT RUN — the recovered assets and provenance record are committed locally; push, merge, and publication remain outside scope.

## Definition of done

- All 13 targets exist and are valid JPEG files at 620×430.
- Every generated basename in `shop.js` resolves to a local file.
- `currently/` is unchanged.
- Repository contract, static contract, work-packet validation, diff check, and JavaScript syntax checks pass.
- The prior residual-risk record is corrected to distinguish capture omission from source absence.
- The four original PNG endpoints remain explicitly documented as unresolved and are not silently replaced.

## Parking lot

- Resolve the four original PNG 404s through a separate CSS/asset decision.
- Add a future automated mirror check that compares generated URLs to the original source.
