# milk planet Web Design Contract

この文書は、AIが`milk planet`の既存ページを改修・HTML化するときに参照する視覚的な正本である。新しいデザインを発明するための仕様ではなく、既存サイトとBranch5の原稿画像から観測できる表現を壊さず再現するための契約とする。

## 正本の優先順位

1. `proposal/branch5/`の同一店舗・同一ページにある画像版
2. `proposal/branch5/`の同一サイト内の隣接ページ
3. `currently/`の同一店舗ページ（旧来の挙動や構造を確認する補助資料）
4. この文書の共通ルール
5. 実装者の一般的なUI慣習

下位のルールが上位の観測結果と衝突する場合は、上位を優先し、店舗固有の例外として記録する。元画像にない見出し、説明、装飾、価格、機能は推測で追加しない。

## Visual thesis

実在店舗の掲示物を、情報の意味と店舗固有の空気感を保ったまま、読みやすく軽いWeb文書へ移す。サイト全体は「統一されたUI」ではなく、共通の導線と異なる店舗表現の集合として扱う。

## 共通の骨格

- ページタイトルは`h1`を1つだけ持ち、店舗ロゴと「システム&メニュー」を同一タイトル領域に置く。
- スクロール後も店舗識別を維持する。ロゴを機械的に極小化しない。
- 本文は`article#menu-content.html-menu`配下に置く。
- セクションは意味単位の`section.menu-section`とし、ページ内ナビゲーションのアンカーと一致させる。
- `h2`はセクションの入口、`h3`はセクション内の分類、`h4`はカード内の小分類に使う。見た目だけの見出しは追加しない。
- `guide`（ご利用案内）は、存在する場合、`special`（限定・グッズ）より後ろに置く。
- 価格と条件は、意味が対応する定義リストまたは意味のあるリストで表現する。視覚上の表だけを`div`の集合で再現しない。

## 共通レイアウトの基準値

これらは出発点であり、元画像・既存ページの観測結果より優先しない。

| 対象 | 広い画面 | 狭い画面 |
| --- | ---: | ---: |
| セクション上下余白 | 54px | 40px |
| セクション見出し下余白 | 42px | 28px |
| 2列グリッド間隔 | 28px | 16px・1列化 |
| 共通カード内余白 | 26px | 20px |
| 本文の基本行間 | 1.5前後 | 1.5前後 |

数値を全店舗へ機械的に適用しない。元画像の密度が高い店舗では余白を詰め、掲示物の余白が特徴の店舗では残す。円形、波形、細い罫線、角形などの特徴は、共通カードへ置き換えず店舗CSSで保持する。

## タイポグラフィと言語

- 英字は既存の`Lato`を基準にする。日本語は既存サイトのシステムフォントを尊重する。
- `h2`の標準値は広い画面で32px、狭い画面で20pxを目安とするが、長い日本語を縮小して押し込まない。
- `word-break:break-all`で語中分断しない。`line-break:strict`、自然な折返し、`wbr`、明示改行を意味境界に限定して使う。
- 商品名、料金、曜日、単位を別情報として扱い、見た目の都合で混ぜない。
- 英語と日本語を混ぜた見出しは、元ページに存在する場合だけ残す。翻訳やキャッチコピーを創作しない。

## 店舗別デザイン変数（Branch5 HTML版）

`proposal/branch5/shop/*/menu-html/system.css`の値を正本とする。値を変更する場合は、元画像または既存店舗ページとの比較理由を`basis/decision_log.md`へ記録する。

| 店舗 | 主色 | 背景 | カード面 | 線 | 角丸 |
| --- | --- | --- | --- | --- | ---: |
| bloody | `#3f0f12` | `#c0e0f7` | `#edf6fc` | `#b9d3e5` | 14px |
| chocolat | `#70181b` | `#fffaf3` | `#fff8ef` | `#d9bc8d` | 4px |
| cybarbkk | `#684092` | `#edfffa` | `#f1fffb` | `#73f9d8` | 2px |
| cybarbkk2 | `#d4295e` | `#fff7fa` | `#fff1f5` | `#ff8bb0` | 18px |
| cybarlaos | `#6f37a5` | `#faf7ff` | `#f5efff` | `#aa00ff` | 3px |
| cybarshinjuku | `#654096` | `#fbf9ff` | `#f5f0ff` | `#bcaeed` | 10px |
| melty | `#d569a9` | `#fff8fc` | `#fff0f8` | `#f2cce1` | 16px |
| roysuga | `#8d5368` | `#ecabbb` | `#fff2f5` | `#d68a9f` | 12px |
| shandy | `#8f0100` | `#fff` | `#f8ecea` | `#e6d4d2` | 8px |
| shinjuku | `#4777c7` | `#cae3f5` | `#edf6ff` | `#9fc4e7` | 14px |
| tweeny | `#8b7047` | `#dff4f9` | `#eefaff` | `#9bcbd8` | 2px |

この表は色の完全なブランドガイドではない。背景画像、ロゴ画像、既存`menu/system.css`の装飾が優先される店舗では、CSS変数だけで再現しようとしない。

## 画像と情報の扱い

- 画像内の装飾・価格・商品配置そのものが価値を持つ場合は、画像を残し、意味のある`alt`を付ける。
- `限定・その他`など、原版をそのまま使うと決めたセクションは、HTMLカードへ分解して二重表示しない。
- 画像をHTMLへ転記する場合も、原稿の情報量を削らない。読みやすさのための分割は、意味の対応関係を壊さない範囲で行う。
- 画像は遅延読み込みを基本とする。ただし、ファーストビューのロゴやタイトル画像を遅延対象にしない。

## AIらしさを抑える禁止事項

- 全店舗を同じ淡色背景・同じ丸角・同じカード幅へ正規化しない。
- 元画像にない「おすすめ」「料金表」などの見出しを、レイアウトの都合で追加しない。
- 具体的な根拠なく、グラデーション、過剰な影、ピル型ボタン、絵文字、装飾アイコンを足さない。
- 余白・フォントサイズ・色を、一般的なデザインテンプレートの値へ一括置換しない。
- 「モダン」「洗練」「かわいい」など抽象的な評価だけで修正を完了扱いにしない。
- 既存の独特な崩し、掲示物由来の密度、店舗ごとの不均一さを、欠陥と決めつけて消さない。

## レビューと証拠

UI変更は、コードだけでなく実表示を確認する。可能ならブラウザで次の幅を取得する。

- 390px前後: スマートフォンの読みやすさ、語中分断、横スクロール、タップ領域
- 768px前後: 列切替、画像幅、タイトル変形、情報密度
- 1440px前後: 元画像版との密度、左右余白、店舗固有の見出し形状

レビュー結果には、対象URL、ビューポート、観測事実、影響、修正、残存リスクを記録する。スクリーンショットは「好みの証明」ではなく、元画像・既存ページとの比較証拠として扱う。

## 完了条件

- 元画像版とHTML版で、情報量・文言・価格・順序が一致している。
- 店舗固有の色、ロゴ、形状、密度が共通テンプレートに埋没していない。
- 見た目の改行が日本語の意味境界と一致している。
- `h1`〜`h4`の階層、`alt`、フォーカス表示、キーボード操作が成立している。
- 390px / 768px / 1440pxで横スクロール、文字欠落、不要な白帯、画像の意図しない左寄せがない。
- 変更理由と店舗固有例外が`basis/decision_log.md`へ記録されている。

## 参照した公開手法

この契約は、既存サイトから`DESIGN.md`を抽出する手法、スクリーンショットによるブラウザレビュー、AIらしい汎用UIを批判的に検出するレビュー手法を、静的なBranch5サイト向けに縮約したものである。

- https://github.com/Paidax01/web-to-design-md
- https://github.com/vltansky/skills/tree/master/skills/design-review
- https://github.com/redblock-ai/langfuse/tree/main/.agents/skills/frontend-browser-review
